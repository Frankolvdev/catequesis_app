Catequesis Android — MegaZIP 02

Extraer dentro de app_2026, donde está settings.gradle.kts. Reemplaza dos archivos Kotlin; no incluye carpeta contenedora.

Añade navegación curso -> clases -> temas, empleando los contratos de la app anterior:
GET /API/v1/course/all, /class_course/all y /theme/all.
Al tocar un tema aún no se abre el contenido de lecciones: se implementará después.

El splash, los iconos y la configuración del mismo Firebase del MegaZIP 01 siguen intactos.
La variante debug utiliza HTTP provisional; release requiere HTTPS válido.

En Android Studio: Sync Project with Gradle Files; ejecutar variante debug;
abrir un curso, una clase y regresar con Atrás o con el botón del teléfono.
Comprobar en git status que app/google-services.json permanece ignorado.
